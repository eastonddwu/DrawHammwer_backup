# tcaplus connection config for dbproxy
# Format: key=value, lines starting with # are comments

# tcaplus App ID
app_id=210

# tcaplus Zone ID
zone_id=1

# tcaplus business signature
signature=EAB20DE87DE4ED4E

# tcaplus dir server address (can specify multiple dir_url lines)
dir_url=tcp://set2.tcapdir.tcaplusdev.oa.com:9999

# tcaplus table names (one per line, each table_name creates a separate entry)
table_name=login
table_name=user_info
table_name=lock_kv
